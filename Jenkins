pipeline {
		agent any
		stages {
				Stage ('Build') {
						steps {
								sh 'echo "Hello World"'
								sh '''
										echo "multiline shell also works"
										ls -lsh
								'''
						}
				}
		}
}
